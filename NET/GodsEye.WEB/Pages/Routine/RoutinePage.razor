@page "/organizacao/rotinas";
@inject ISnackbar Snackbar

<PageTitle>Rotinas</PageTitle>

<div class="page-container">
    <div class="breadcrumb-container">
        <MudBreadcrumbs Style="padding: 8px 0px; font-size: 12px; font-weight: 500" Items="_items"></MudBreadcrumbs>
    </div>

    <div class="content-container">

        <div class="page-title">
            <h1>Setores</h1>
            <p>Aqui você encontra uma lista das rotinas cadastradas em seu sistema</p>
        </div>

        <div class="notification-menu">
            <div class="filters">
                <div class="option">
                    <MudTextField @bind-Value="_routineFilterName" TextChanged="ApplyFilters" Immediate="true" Label="Setor" Variant="Variant.Outlined" Margin="Margin.Dense" Clearable="true" />
                </div>
            </div>

            <MudButton Variant="Variant.Outlined" OnClick="() => DialogWebService.OpenCreateRoutine(CraeteRoutineCallback)">Criar Rotina</MudButton>
        </div>

        <MudTable Items="@_filteredRoutines" Hover="true" Loading="@_loading" Elevation="0" LoadingProgressColor="Color.Info">
            <HeaderContent>
                <MudTh>Nome</MudTh>
            </HeaderContent>
            <RowTemplate>
                <MudTd DataLabel="Name">@context.Name</MudTd>
            </RowTemplate>
        </MudTable>
    </div>

    
</div>

