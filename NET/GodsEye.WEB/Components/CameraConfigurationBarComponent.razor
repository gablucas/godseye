<div>
    <MudChip T="string" Size="Size.Large" Style="padding: 8px;" Href=@($"/camera/{Id}/dados")" Label="true" Color=@(Pagina == "dados" ? Color.Success : Color.Default)>
        <MudText Typo="Typo.subtitle1" Class="m-0">
            Dados
        </MudText>
    </MudChip>

    <MudChip T="string" Size="Size.Large" Style="padding: 8px;" Href=@($"/camera/{Id}/reconhecimento")" Label="true" Color=@(Pagina == "reconhecimento" ? Color.Success : Color.Default)>
        <MudText Typo="Typo.subtitle1" Class="m-0">
            Reconhecimento
        </MudText>
    </MudChip>

    @if (HasFeature(1))
    {
        <MudChip T="string" Size="Size.Large" Style="padding: 8px;" Href=@($"/camera/{Id}/monitoramento-ambiente")" Label="true" Color=@(Pagina == "monitoramento-ambiente" ? Color.Success : Color.Default)>
            <MudText Typo="Typo.subtitle1" Class="m-0">
                Monitoramento de ambientes
            </MudText>
        </MudChip>

    }

    @if (HasFeature(2))
    {
        <MudChip T="string" Size="Size.Large" Style="padding: 8px;" Href=@($"/camera/{Id}/registro-incidente")" Label="true" Color=@(Pagina == "registro-incidente" ? Color.Success : Color.Default)>
            <MudText Typo="Typo.subtitle1" Class="m-0">
                Registro de incidentes
            </MudText>
        </MudChip>
    }

    @if (HasFeature(3))
    {
        <MudChip T="string" Size="Size.Large" Style="padding: 8px;" Href=@($"/camera/{Id}/controle-permanencia")" Label="true" Color=@(Pagina == "controle-permanencia" ? Color.Success : Color.Default)>
            <MudText Typo="Typo.subtitle1" Class="m-0">
                Controle de permanência
            </MudText>
        </MudChip>
    }
</div>