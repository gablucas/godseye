CREATE DEFINER=`root`@`localhost` PROCEDURE `SP_COMPLIANCE_GET_BY_ID`(
	IN P_COMPLIANCE_ID INT
)
BEGIN
    SELECT 
		CP.ID,
		CP.NAME,
        CP.CREATED_AT,
        CP.UPDATED_AT
	FROM COMPLIANCE_POLICY CP
    WHERE CP.ID = P_COMPLIANCE_ID;
END